## 232. Implement Queue using Stacks
- 题目链接：https://leetcode.com/problems/implement-queue-using-stacks/
- 文章讲解/视频讲解： 

### 💭 First Thought
- 两个list来调整stack in 和out 的顺序

### ✅ Final Approach

```
    def __init__(self):
        self.stack_in = []
        self.stack_out = []

    def push(self, x: int) -> None:
        self.stack_in.append(x)

    def pop(self) -> int:
        if self.empty():
            return None
        if self.stack_out:
            return self.stack_out.pop()
        else:
            for i in range(len(self.stack_in)):
                self.stack_out.append(self.stack_in.pop())
            return self.stack_out.pop()

    def peek(self) -> int:
        res = self.stack_out[-1] if self.stack_out else self.stack_in[0]
        return res
        

    def empty(self) -> bool:
        return not (self.stack_in or self.stack_out)
```

### ⚠️ Mistakes / Gotchas

### Complexity
Time: O(n)

&nbsp;
## 225. Implement Stack using Queues
- 题目链接：https://leetcode.com/problems/implement-stack-using-queues/description/
- 文章讲解/视频讲解： 

### 💭 First Thought
- 两个list来调整stack in 和out 的顺序

### ✅ Final Approach

```
class MyStack:

    def __init__(self):
        self.stack = []

    def push(self, x: int) -> None:
        self.stack.append(x)

    def pop(self) -> int:
        if self.empty():
            return None
        return self.stack.pop()

    def top(self) -> int:
        if self.empty():
            return None
        res = self.stack[-1]
        return res

    def empty(self) -> bool:
        return True if len(self.stack) == 0 else False
        
```

### ⚠️ Mistakes / Gotchas

### Complexity
Time: O(n)

&nbsp;


## 20. Valid Parentheses
- 题目链接：https://leetcode.com/problems/valid-parentheses/description/
- 文章讲解/视频讲解： 

### 💭 First Thought
- 运用stack 和 dic 来store 每一个character
### ✅ Final Approach

```
    def isValid(self, s: str) -> bool:
        stack = []
        dic = {")":"(", "}":"{", "]":"["}
        for c in s:
            if c in dic:
                if len(stack) == 0:
                    return False
                else:
                    res = stack.pop()
                    if res != dic[c]:
                        return False
            else:
                stack.append(c)
        return len(stack) == 0 
        
```

### ⚠️ Mistakes / Gotchas
- 需要注意当stack为空的时候无法pop， 也许调整一下if的顺序会更合理
### Complexity
Time: O(n)

&nbsp;


## 1047. Remove All Adjacent Duplicates In String
- 题目链接：https://leetcode.com/problems/remove-all-adjacent-duplicates-in-string/description/
- 文章讲解/视频讲解： 

### 💭 First Thought
- 运用stac
### ✅ Final Approach

```
    def removeDuplicates(self, s: str) -> str:
        tem = []
        for c in s:
            if len(tem) == 0:
                tem.append(c)
            else:
                res = tem[-1]
                if res == c:
                    tem.pop()
                else:
                    tem.append(c)
        return "".join(tem)
        
```

### ⚠️ Mistakes / Gotchas

### Complexity
Time: O(n)

&nbsp;
