# 2026-01-14 数组part01

## 数组理论基础
- 数组是存放在连续内存空间上的相同类型数据的集合
- 数组下标都是从0开始
- 数组内存空间的地址是连续的
- 二维数组可能连续（C++), 也可能不连续

## 🧩  704. Binary Search 
- 题目链接：https://leetcode.com/problems/binary-search/description/
- 文章讲解：https://programmercarl.com/0704.%E4%BA%8C%E5%88%86%E6%9F%A5%E6%89%BE.html
- 视频讲解：https://www.bilibili.com/video/BV1fA4y1o715


### 💭 First Thought
- 使用二分法， 左右两个指针划分区间与target值比较，直到找到target的位置
- 需要思考退出条件， 是start < end 还是等于。
- 需要思考每次边界值的处理

### ✅ Final Approach
  1. 左闭右开

  ```
    def search(self, nums: List[int], target: int) -> int:
        start, end = 0, len(nums)
        while start < end:
            mid = start + (end - start) // 2
            if nums[mid] < target:
                start = mid + 1
            elif nums[mid] > target:
                end = mid
            else: 
                return mid
        return -1
  ```
     
  2. 左闭右闭

```
    def search(self, nums: List[int], target: int) -> int:
        start, end = 0, len(nums) - 1
        while start <= end:
            mid = start + (end - start) // 2
            if nums[mid] < target:
                start = mid + 1
            elif nums[mid] > target:
                end = mid - 1
            else: 
                return mid
        return -1

```

### ⚠️ Mistakes / Gotchas

- 没有想到有两种不同的解法， 想当然地用了 start < end 作为退出条件， 但应该根据start 和end 的初始值来判断
- 最初直接用了mid = (start + end) //2, 但会有溢出问题， 应该使用mid = start + (end - start) // 2
  > 如果（start+ end) 大于 int max，就会无法显示


### ⏱ Complexity
  O(logn)

