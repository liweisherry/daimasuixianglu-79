##  链表理论基础 
- 链表是一种通过指针串联在一起的线性结构，每一个节点由两部分组成，一个是数据域一个是指针域（存放指向下一个节点的指针），最后一个节点的指针域指向null
- 如何构造节点：
  ```
  class ListNode:
    def __init__(self, val, next=None):
        self.val = val
        self.next = next
  ```


## 🧩  203. Remove Linked List Elements
- 题目链接：https://leetcode.com/problems/remove-linked-list-elements/
- 视频讲解：https://www.bilibili.com/video/BV18B4y1s7R9/?share_source=copy_web&vd_source=de126c758f7d00dc488ba9cff60a9554



### 💭 First Thought
- 需要考虑的是如果移除的是头节点，该怎么处理：
    1. 可以设置一个虚拟节点， 然后指向下一个作为head
    2. 在原列表上直接处理，通过分类来处理头节点
- 

### ✅ Final Approach
```
    def removeElements(self, head: Optional[ListNode], val: int) -> Optional[ListNode]:
        if head is None:
            return head 
        dummy = ListNode(next = head)
        p = dummy


        while p.next:
            if p.next.val == val:

                p.next = p.next.next
            else:
                p = p.next
            
        return dummy.next
```
```
     def removeElements(self, head: Optional[ListNode], val: int) -> Optional[ListNode]:
        if not head:
            return None
        cur = head
        pre = None
        while cur:
            if cur.val == val:
                if cur == head:
                    head = cur.next
                else:
                    pre.next = cur.next 
                cur = cur.next
            else:
                pre = cur
                cur = cur.next
        return head
```

### ⚠️ Mistakes / Gotchas
- 一开始没有搞清楚p是指向比较需要移除的元素之前的那一个元素， 写成了p.val = val

### Complexity
Time: O(n)


### ⏱ Complexity
Time:
